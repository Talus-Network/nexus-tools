# {{crate_name}} — required environment variables.
#
# Copy to `.env` for local dev (this file MUST stay in source control;
# `.env` MUST be gitignored). In production these come from Cloud Run
# `secretKeyRef` bindings to GCP Secret Manager (operator-configured).
#
# The tool fails to start without {{SERVICE_UPPER}}_API_KEY set.

{{SERVICE_UPPER}}_API_KEY=sk_test_replace_with_real_key

# Optional. Defaults to "info".
RUST_LOG=info
